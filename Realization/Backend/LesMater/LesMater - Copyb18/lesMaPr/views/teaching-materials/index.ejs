<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/product.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

            <main>
                <% if (materials.length> 0) { %>
                    <div class="grid">
                        <% for (let material of materials) { %>
                            <article class="card material-item">
                                <header class="card__header">
                                    <h1 class="material__title">
                                        <%= material.title %>
                                    </h1>
                                </header>
                                <div class="card__image">
                                    <img src="<%= material.imageUrl %>" alt="<%= material.title %>">
                                </div>
                                <div class="card__content">
                                    <h2 class="material__price">$<%= material.price %>
                                    </h2>
                                    <p class="material__description">
                                        <%= material.description %>
                                    </p>
                                </div>
                                <div class="card__actions">
                                    <a href="teaching-materials/<%= material._id %>" class="btn">Details</a>
                                </div>
                            </article>
                            <% } %>
                    </div>
                    <% } else { %>
                        <h1>No teaching materials!</h1>
                        <% } %>
            </main>
            <%- include('../includes/end.ejs') %>